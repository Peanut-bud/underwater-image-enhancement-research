project:
  name: underwater_image_enhancement
  stage: stage2_model

data:
  input_channels: 3

model:
  name: "physical_guided_enhancer"
  base_channels: 32
  estimator_channels: 32
  refinement_blocks: 2
  t_min: 0.05

training:
  enabled: false

loss:
  enabled: false

output:
  note: "Physical-guided model base config."

runtime:
  device: "cpu"
